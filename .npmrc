# Stable and canary packages are public npm packages. Local beta publication
# passes Verdaccio's URL explicitly, so it cannot move a public npm dist-tag.
@ackerdb:registry=https://registry.npmjs.org/
access=public
ignore-scripts=true
