# Route the @dbzz scope to the local Verdaccio registry (see AGENTS.md "Local Publishing").
# bun publish resolves the target registry from this line; auth token lives in ~/.npmrc
# (created once via: bunx npm adduser --registry http://127.0.0.1:4874).
@dbzz:registry=http://127.0.0.1:4874
