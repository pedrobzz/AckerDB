/**
 * Optional Canvazz configuration. Export a function returning a config object.
 * Everything here is optional — delete this file to use the defaults.
 */
export default function config() {
  return {
    /** Directory the canvas crawler scans for `**\/*.tsx` pages. */
    srcDir: "src",
  };
}
