# local research / docs workspace (not part of the published repo)
GOAL.md
raw
wiki
opensrc
.wiki
.research
.agents
.claude
skills-lock.json

# deps / build
node_modules
dist
*.log
.DS_Store

# dbzz local databases
.zdb/

# local demo monorepo (kept out of the repo on purpose)
sandbox/

# benchmark artifacts (local backend state, generated bindings)
# bench/results/ is tracked on purpose: it is the cross-run comparison history
bench/convex-app/.convex/
bench/convex-app/.env.local
bench/convex-app/convex/_generated/
bench/dbzz-app/_generated/
bench/spacetime-app/.stdb-data/
bench/spacetime-app/module_bindings/
