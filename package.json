{
  "name": "dbzz",
  "private": true,
  "workspaces": [
    "packages/*",
    "fixtures/*"
  ],
  "scripts": {
    "test": "bun test ./packages/core/test ./packages/server/test ./packages/cache/test ./packages/client/test ./packages/cli/test && bun test ./packages/client-react/test && bun scripts/mcp-conformance.ts && bun scripts/verify-packages.ts",
    "test:mcp:conformance": "bun scripts/mcp-conformance.ts",
    "test:mcp:hosts": "bun scripts/mcp-host-acceptance.ts",
    "test:packages": "bun scripts/verify-packages.ts",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit && bunx tsc -p packages/client-react/tsconfig.json --noEmit",
    "typecheck:bench": "bunx tsc -p bench/tsconfig.json --noEmit",
    "diagnostic:validator": "bun scripts/validator-diagnostic.ts",
    "bench:hetzner": "bun scripts/bench-hetzner.ts",
    "registry": "bunx verdaccio --config registry/config.yaml",
    "bump": "bun scripts/bump.ts",
    "publish:local": "bun scripts/publish-local.ts",
    "publish:beta": "bun scripts/publish-beta.ts",
    "prepare": "git config core.hooksPath .githooks && git config branch.main.mergeoptions --no-ff"
  },
  "devDependencies": {
    "@modelcontextprotocol/conformance": "0.1.16",
    "@types/bun": "^1.2.0",
    "@types/node": "22.15.3",
    "typescript": "^5.9.0"
  }
}
