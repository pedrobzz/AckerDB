name: Pull request

on:
  pull_request:
    branches: [canary, main]
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

concurrency:
  group: pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  changes:
    name: Select affected work
    # A promotion (`canary` → `main`) selects work too, but only so `Benchmark`
    # can read `performance`: everything else was already proven on the way into
    # `canary`, and `Fast CI` stays skipped there as before.
    if: github.event.pull_request.draft == false && (github.base_ref == 'canary' || github.base_ref == 'main' || github.head_ref != 'canary')
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      test_packages: ${{ steps.changes.outputs.test_packages }}
      code: ${{ steps.changes.outputs.code }}
      native: ${{ steps.changes.outputs.native }}
      performance: ${{ steps.changes.outputs.performance }}
      telemetry: ${{ steps.changes.outputs.telemetry }}
      verify_packages: ${{ steps.changes.outputs.verify_packages }}
      mcp: ${{ steps.changes.outputs.mcp }}
      workflows: ${{ steps.changes.outputs.workflows }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          fetch-depth: 0
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
        with:
          bun-version-file: .bun-version
      - id: changes
        run: >-
          bun scripts/ci/changes.ts
          "${{ github.event.pull_request.base.sha }}"
          "${{ github.event.pull_request.head.sha }}"

  native:
    name: Native
    needs: changes
    if: needs.changes.outputs.native == 'true'
    uses: ./.github/workflows/native.yml

  fast-ci:
    name: Fast CI
    if: always()
    needs: [changes, native]
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - name: Validate prerequisite jobs
        env:
          CHANGES: ${{ needs.changes.result }}
          NATIVE: ${{ needs.native.result }}
        run: |
          case "$CHANGES" in success|skipped) ;; *) exit 1 ;; esac
          case "$NATIVE" in success|skipped) ;; *) exit 1 ;; esac
      - if: needs.changes.result == 'success'
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          fetch-depth: 0
      - if: needs.changes.result == 'success'
        uses: ./.github/actions/bun-workspace
      - name: Test affected packages
        if: needs.changes.result == 'success' && needs.changes.outputs.test_packages != '[]'
        env:
          TEST_PACKAGES: ${{ needs.changes.outputs.test_packages }}
        run: bun scripts/ci/test-affected.ts
      - name: Typecheck packages
        if: needs.changes.outputs.code == 'true'
        run: bun run typecheck
      - name: Check repository boundaries
        if: needs.changes.outputs.code == 'true'
        run: |
          bun test scripts/ci scripts/release
          bun test ./bench
          bun run typecheck:tooling
          bun run typecheck:bench
      - name: Audit production dependencies
        if: needs.changes.outputs.verify_packages == 'true'
        run: bun audit --prod
      - name: Check MCP conformance
        if: needs.changes.outputs.mcp == 'true'
        run: bun run test:mcp:conformance
      - name: Verify publishable packages
        if: needs.changes.outputs.verify_packages == 'true'
        run: bun run test:packages
      - name: Lint workflows
        if: needs.changes.outputs.workflows == 'true'
        uses: rhysd/actionlint@914e7df21a07ef503a81201c76d2b11c789d3fca

  # The benchmark gates the release, not the road to it. A merge into `canary`
  # publishes a prerelease nobody depends on; a promotion to `main` ships. So it
  # runs on the promotion only, where base is `main` and head is `canary` — which
  # makes the comparison the whole release delta rather than one pull request's
  # slice, the only measurement that sees what twenty merges did together. Every
  # pull request into `canary` stops paying for it.
  #
  # `always()` with the prerequisite gate below mirrors Fast CI: a required check
  # must report a conclusion even when it has nothing to measure, or a promotion
  # touching no performance-relevant input would sit pending forever.
  benchmark:
    name: Benchmark
    if: always()
    needs: [changes, native]
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - name: Validate prerequisite jobs
        env:
          CHANGES: ${{ needs.changes.result }}
          NATIVE: ${{ needs.native.result }}
        run: |
          case "$CHANGES" in success|skipped) ;; *) exit 1 ;; esac
          case "$NATIVE" in success|skipped) ;; *) exit 1 ;; esac
      - if: github.base_ref == 'main' && needs.changes.outputs.performance == 'true'
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          fetch-depth: 0
      - if: github.base_ref == 'main' && needs.changes.outputs.performance == 'true'
        uses: ./.github/actions/bun-workspace
      - name: Measure the release delta
        if: github.base_ref == 'main' && needs.changes.outputs.performance == 'true'
        env:
          BENCH_EXECUTION_HOST: github-hosted
          BENCH_TELEMETRY_PROFILES: ${{ needs.changes.outputs.telemetry == 'true' && 'enabled,exporter,disabled' || 'disabled' }}
        run: >-
          bun bench/compare-commits.ts
          "${{ github.event.pull_request.base.sha }}"
          "${{ github.event.pull_request.head.sha }}"
          benchmark-results
      - name: Write the benchmark reasoning input
        if: github.base_ref == 'main' && needs.changes.outputs.performance == 'true'
        run: bun bench/report.ts benchmark-results/base.json benchmark-results/head.json | tee benchmark-results/comparison.md >> "$GITHUB_STEP_SUMMARY"
      - if: github.base_ref == 'main' && needs.changes.outputs.performance == 'true'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: ackerdb-benchmark-${{ github.event.pull_request.head.sha }}
          path: benchmark-results
          if-no-files-found: error
          retention-days: 30
